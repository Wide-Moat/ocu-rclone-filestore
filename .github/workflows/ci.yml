# SPDX-License-Identifier: FSL-1.1-Apache-2.0
# Copyright (c) 2025 Open Computer Use Contributors
#
# Core build/test gate. Every pull request (and push to main) builds for
# linux/amd64 and linux/arm64, vets, tests with coverage, ratchets coverage
# against the recorded floor, asserts the build-graph denylist, and checks
# that every authored file carries the FSL header.

name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goos: [linux]
        goarch: [amd64, arm64]
    env:
      GOOS: ${{ matrix.goos }}
      GOARCH: ${{ matrix.goarch }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
      - name: Build (cross-compile, build-only)
        run: go build ./...

  test:
    name: vet + test + coverage ratchet
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
      - name: Vet
        run: go vet ./...
      - name: Test with race + coverage
        # -coverpkg=./... puts every package in the coverage denominator, so a
        # package added without tests lowers the total instead of being
        # invisible to the ratchet.
        run: go test ./... -race -coverprofile=cover.out -coverpkg=./...
      - name: Coverage ratchet
        env:
          COVERAGE_PROFILE: cover.out
        run: go test ./internal/coverage/ -run TestCoverageRatchet -count=1

  fuzz-smoke:
    name: fuzz smoke (defensive parsers)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
      # Bounded fuzz of the two untrusted-byte trust surfaces: the host-supplied
      # mount config (mountcfg) and the broker wire frames/headers (brokerrpc).
      # The PR gate runs ONLY the committed seed corpus (deterministic): a seed
      # regression fails the PR, but no stochastic exploration runs here, so an
      # unrelated PR can never trip a newly-discovered crash and go red at random.
      # Exploratory fuzzing with a real per-target time budget runs nightly in
      # fuzz.yml (schedule + workflow_dispatch), which enumerates the targets at
      # run time; the per-PR gate here stays deterministic on purpose.
      - name: Fuzz-seed broker-wire parsers (brokerrpc)
        run: |
          set -euo pipefail
          # -run=Fuzz.* executes EVERY FuzzXxx target's committed seed corpus as a
          # normal test in one invocation (deterministic). Naming targets by an
          # allowlist risks drift — a renamed target would silently stop running —
          # so match them all and let the run cover whatever fuzz targets exist.
          go test ./internal/brokerrpc/ -run='Fuzz'
      - name: Fuzz-seed host-config loader (mountcfg)
        run: |
          set -euo pipefail
          go test ./internal/mountcfg/ -run='Fuzz'

  denylist:
    name: build-graph denylist
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
      - name: No foreign object-store client in the dependency graph
        run: go test ./internal/buildgraph/ -run TestNoForeignObjectStore -count=1

  fsl-headers:
    name: authored-file FSL-header gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check FSL headers
        run: bash scripts/check-fsl-headers.sh

  # Frozen-contract binding for the vendored mount-config schema. Two legs run
  # here: the hermetic checksum leg proves the vendored bytes match the
  # committed sha256 pin (tamper-evidence: silent drift becomes a conspicuous
  # multi-file diff), and the canon-ref leg proves the same bytes exist at the
  # public canon commit recorded in testdata/CANON-REF (arbitrary non-canon
  # bytes cannot pass). The canon repository is public, so the fetch needs no
  # secret and the job is fork-safe. Non-required until the owner adds it to
  # branch protection; the pin is also enforced by the hermetic
  # TestVendoredSchemaChecksumPin under the required test gate.
  contract-pin:
    name: frozen-contract checksum pin
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # This job only reads files and runs the parity script — no git
          # operations after checkout — so it does not need the token persisted
          # in the local git config.
          persist-credentials: false
      - name: Verify the vendored contract against its pin and canon ref
        env:
          OCU_CANON_REF_CHECK: "1"
        run: bash scripts/check-contract-parity.sh

  doc-contacts:
    name: documentation contact-address gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check documentation contact address
        run: bash scripts/check-doc-contacts.sh

  doc-slop:
    name: documentation anti-slop gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Self-test the doc-slop scanner
        # Run the scanner's own test first: it proves the gate detects each slop
        # tell and spares the allowed forms, so a regression in the scanner
        # fails here rather than silently passing real docs.
        run: bash scripts/check-doc-slop_test.sh
      - name: Check documentation for slop tells
        run: bash scripts/check-doc-slop.sh

  # WR-03: probe whether the hosted runner grants /dev/fuse to a container
  # running with the same device/capability pattern the e2e harness uses.
  # This job is INFORMATIONAL — it records the placement evidence in the job
  # summary on every run and never fails the PR. The HARD enforcement lives on
  # the release path: release.yml's e2e job requires /dev/fuse and fails red
  # without it (no continue-on-error, no bypass), so when the hosted runner
  # cannot mount FUSE the live gate must run on a self-hosted/Lima host.
  # See docs/ci-fuse-decision.md for the decision record.
  fuse-probe:
    name: /dev/fuse availability probe
    runs-on: ubuntu-latest
    # Expose the container-FUSE result so the live-e2e job below runs only where
    # a real FUSE mount is possible, and skips cleanly (not red) where it is not.
    outputs:
      container_fuse: ${{ steps.probe.outputs.container_fuse }}
    steps:
      # The smoke performs a REAL FUSE mount inside a container granted only
      # /dev/fuse + SYS_ADMIN (AppArmor unconfined, mirroring the harness
      # posture), using the public rclone image pinned by digest — rclone is
      # the public project this binary openly builds on, and its image can
      # mount an in-memory remote with no credentials and no network.
      # rclone/rclone:1.74.3.
      - name: Probe the device and a container FUSE mount
        id: probe
        run: |
          host_fuse=no
          container_fuse=no
          if [ -e /dev/fuse ]; then
            host_fuse=yes
            if docker run --rm \
                 --device /dev/fuse --cap-add SYS_ADMIN \
                 --security-opt apparmor=unconfined \
                 --entrypoint /bin/sh \
                 rclone/rclone@sha256:623378ad0ff3ebd5cebf77720843c0e02edfe46e2d5b5ac6bed54c6371780dfb \
                 -c 'set -e; mkdir -p /probe; rclone mount :memory: /probe --daemon; grep -q " /probe fuse" /proc/mounts'; then
              container_fuse=yes
            fi
          fi
          {
            echo "## /dev/fuse probe (WR-03)"
            echo ""
            echo "| check | result |"
            echo "| --- | --- |"
            echo "| /dev/fuse present on the hosted runner | ${host_fuse} |"
            echo "| real FUSE mount inside a /dev/fuse + SYS_ADMIN container | ${container_fuse} |"
            echo ""
            if [ "${container_fuse}" = yes ]; then
              echo "The hosted runner can run the live e2e gate: release.yml's e2e job runs here as-is."
            else
              echo "The hosted runner cannot run the live e2e gate: the release-path e2e job fails hard on this host by design and must run on a self-hosted/Lima host (docs/ci-fuse-decision.md)."
            fi
          } >> "${GITHUB_STEP_SUMMARY}"
          if [ "${container_fuse}" = yes ]; then
            echo "::notice::hosted runner FUSE probe: container FUSE mount works; the live e2e gate can run in GitHub CI"
          else
            echo "::warning::hosted runner FUSE probe: container FUSE mount unavailable; the live e2e gate must run on a self-hosted/Lima host (the release-path job fails hard there by design)"
          fi
          echo "container_fuse=${container_fuse}" >> "${GITHUB_OUTPUT}"

  # Live e2e gate on pull requests. It runs the SAME real harness as the
  # release-path e2e (the network-topology graph — harness peers + the live edge
  # + the mount with /dev/fuse + SYS_ADMIN + the test-runner driving the full
  # gated exercise, including the Envoy-only-hop assertion, the cold read through
  # a second fresh mount and the SC2 throttle), so a PR that touches the data
  # path is verified live before merge — not only at release.
  #
  # It runs ONLY when the fuse-probe above confirms the hosted runner can mount
  # FUSE in a container; otherwise it skips cleanly (a skipped required check is
  # treated as passed by branch protection, so this never deadlocks a PR for a
  # capability reason). On a host that CAN mount FUSE, a real e2e failure fails
  # the job and blocks the merge. The release-path e2e (release.yml) remains the
  # hard, non-skippable gate that fail-closes a publish.
  live-e2e:
    name: live e2e (data path)
    runs-on: ubuntu-latest
    needs: [fuse-probe]
    if: needs.fuse-probe.outputs.container_fuse == 'yes'
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      # Every mount destination is a host bind (at the canonical /mnt/user-data
      # mount root) with rshared propagation; the subdirectories must exist
      # before the harness starts (the mount binary does not create them).
      # outputs = rw scope, uploads = ro scope, outputs2 = the cold-read second
      # mount of the rw scope, throttle = the SC2 throttle scope.
      #
      # Recreated FRESH each run: the mount refuses to shadow residue left by
      # an aborted previous run (files under the bind are indistinguishable
      # from real user data), so any stale scope mount is lazy-unmounted FIRST
      # (an rm through a live stale FUSE mount would delete broker-side
      # content), then the root is removed and re-created dirs-only.
      - name: Prepare the mount-root bind
        run: |
          for d in outputs uploads outputs2 throttle; do
            sudo umount -l "/tmp/ocu-e2e-mountroot/$d" 2>/dev/null || true
          done
          sudo rm -rf /tmp/ocu-e2e-mountroot
          mkdir -p /tmp/ocu-e2e-mountroot/outputs /tmp/ocu-e2e-mountroot/uploads /tmp/ocu-e2e-mountroot/outputs2 /tmp/ocu-e2e-mountroot/throttle
      # The mount service references a NAMED AppArmor profile (apparmor=ocu-mount).
      # A named profile must be loaded into the runner's host kernel before the
      # container is created, or OCI create fails ("unable to apply apparmor
      # profile ... no such file or directory", exit 127) and the mount never
      # starts. ubuntu-latest ships AppArmor and apparmor_parser; load the profile
      # here. No swallowing: a parser rejection is a real defect we want surfaced,
      # so the step fails loudly rather than continuing to an exit-127 bring-up.
      - name: Load the mount AppArmor profile into the runner kernel
        run: sudo apparmor_parser -r -W deploy/compose/apparmor/ocu-mount.profile
      - name: Bring up the harness (peers + edge + mount)
        run: docker compose -f deploy/compose/docker-compose.yml up --build -d harness-init control-plane exchange filestore edge mount
      # The one-shot posture probe witnesses the kernel-enforced read_only-rootfs
      # (EROFS) and tmpfs-writability arms from INSIDE its own hardened namespace.
      # The test-runner in a different namespace/uid cannot write through the
      # mount's procfs root — the kernel denies it with EACCES before the mount
      # table's EROFS/tmpfs semantics apply. Run the probe to completion so its
      # verdict exists on the shared volume before the test-runner reads it; a
      # non-zero verdict (either arm failed) fails this step loudly.
      - name: Witness the mount runtime posture (one-shot probe)
        run: docker compose -f deploy/compose/docker-compose.yml run --rm --build mount-posture-probe
      - name: Run the live exercise (test-runner service)
        run: docker compose -f deploy/compose/docker-compose.yml run --rm --build test-runner
      - name: Run the backend conformance round-trip (conformance-runner service)
        run: docker compose -f deploy/compose/docker-compose.yml run --rm --build conformance-runner
      - name: Harness logs on failure
        if: failure()
        run: docker compose -f deploy/compose/docker-compose.yml logs
      - name: Teardown
        if: always()
        run: docker compose -f deploy/compose/docker-compose.yml down -v
